public class FormController {
	@AuraEnabled
    public static void SaveContact(Contact con) {
        system.debug('Here is the Contact record UPSERTED to Contact-Object: ' + con);
        upsert con;
    }
} //Apex Controller makes DML calls to Org